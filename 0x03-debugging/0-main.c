i#include "main.h"
/**
 * main - it runs a test for positive and negative function
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
	int test;

	test = 0;
	positive_or_negative(test);
	return (0);
}
